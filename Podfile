# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
platform :osx, '10.12'

target 'GoogleTranslateBar' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GoogleTranslateBar
  pod 'ROGoogleTranslate', '~> 0.0'

  target 'GoogleTranslateBarTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GoogleTranslateBarUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
